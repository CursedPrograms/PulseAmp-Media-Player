# Build output (CMake build folders: object files, not the release)
build/
out/
.vs/

# Releases in dist/ (the zips and installer) ARE committed: the GitHub Pages
# site links to them. The unpacked folder and the download cache are not.
dist/.cache/
dist/PulseAmp/
installer/*.exe
